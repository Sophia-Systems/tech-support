FROM python:3.12-slim AS base

WORKDIR /app

RUN pip install uv

COPY pyproject.toml ./
RUN uv sync --no-dev --no-install-project

COPY config/ ./config/
COPY app/ ./app/

CMD ["uv", "run", "arq", "app.workers.ingestion_worker.WorkerSettings"]
